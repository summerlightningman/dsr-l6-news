import styled from 'styled-components';

const TagListItemName = styled.span`
  padding: 15px 0;
  font-family: 'Inter', sans-serif;
  font-style: normal;
  font-weight: 700;
  font-size: 32px;
  line-height: 39px;
  display: flex;
  align-items: center;
`;

export default TagListItemName